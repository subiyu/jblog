<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="category">
	<insert id="insert" parameterType="map">
		<![CDATA[
		INSERT INTO category VALUES (null, #{name }, #{description }, current_date(), #{blogId })
		]]>
		<selectKey keyProperty="no" resultType="long" order="AFTER">
			<![CDATA[
			SELECT last_insert_id() FROM dual
			]]>
		</selectKey>
	</insert>
	
	<select id="findAll" parameterType="string" resultType="categoryvo">
		<![CDATA[
		 SELECT no, name, description, date_format(reg_date, '%Y/%m/%d') as regDate, blog_id as blogId 
		 FROM category 
		 WHERE blog_id = #{blogId }
		]]>
	</select>
</mapper>
